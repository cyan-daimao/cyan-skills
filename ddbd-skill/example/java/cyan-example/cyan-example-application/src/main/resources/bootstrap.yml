# 基础应用配置
spring:
  profiles:
    active: dev
  application:
    name:  cyan-example
  main:
    web-application-type: servlet
# 服务监控端点（暴露所有端点，生产环境需调整）
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

